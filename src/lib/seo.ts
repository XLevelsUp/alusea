// Shared SEO constants — single source of truth for the canonical domain.
// Import this in sitemap.ts, robots.ts, and anywhere else a hardcoded domain is needed.
export const BASE_URL = "https://www.alusea.in";
